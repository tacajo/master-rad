import { StyleSheet } from "aphrodite/no-important";

export const AboutStyle = StyleSheet.create({
  logo: {
    display: "block",
    maxWidth: 300,
    width: "100%",
    marginBottom: 15,
  },
});
