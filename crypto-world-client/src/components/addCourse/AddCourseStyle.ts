import { StyleSheet } from "aphrodite/no-important";
import { COLORS, FONT_WEIGHT } from "../../assets/style/variables";

export const AddCourseStyle = StyleSheet.create({
  modalDesign: {},
  title: {
    color: COLORS.white,
  },
});
